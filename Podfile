# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'UniversalLogger' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UniversalLogger
    pod 'Alamofire', '~> 4.7'
    pod 'SwiftyJSON', '~> 4.0'
    pod "PromiseKit", "~> 6.0"
    pod 'SSSpinnerButton'
    pod 'ReachabilitySwift'
    pod 'SwiftKeychainWrapper'
    pod "DynamicBlurView"
    pod "SearchTextField"
    pod 'SwiftyStoreKit'


  target 'UniversalLoggerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UniversalLoggerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
